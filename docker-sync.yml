version: '2'

options:
  verbose: false

syncs:
  my_first_neo4j-sync:
    src: './'
    sync_strategy: 'native_osx'
    notify_terminal: true
    sync_excludes:
      - '.bundle'
      - '.git'
      - '.gitignore'
      - '.DS_Store'
